[package]
name = "dtools"
version = "0.1.0"
authors = ["DCjanus <DCjanus@dcjanus.com>"]
edition = "2024"
rust-version = "1.85"
default-run = "dtools"
description = "用于处理文本、维护 Git 仓库和辅助 Coding Agent 的小型命令行工具集"
repository = "https://github.com/DCjanus/dtools"
license = "MIT"
readme = "README.md"
keywords = ["cli", "git", "sort", "text", "uniq"]
categories = ["command-line-utilities", "text-processing"]

[package.metadata.binstall]
pkg-url = "{ repo }/releases/download/latest/dtools-{ target }{ archive-suffix }"
bin-dir = "dtools-{ target }/{ bin }{ binary-ext }"

[package.metadata.binstall.overrides.'cfg(target_os = "windows")']
pkg-fmt = "zip"

[package.metadata.binstall.overrides.'cfg(not(target_os = "windows"))']
pkg-fmt = "tgz"

[dependencies]
anyhow = "1.0.103"
clap = { version = "4.6.1", features = ["derive"] }
clap_complete = { version = "4.6.7", features = ["unstable-dynamic"] }
globset = "0.4.19"
size = "0.5.0"
tempfile = "3.27.0"

[profile.release]
lto = "thin"
strip = true
